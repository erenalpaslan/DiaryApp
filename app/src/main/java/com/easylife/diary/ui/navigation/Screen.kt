package com.easylife.diary.ui.navigation

enum class Screen(val route: String) {
    Splash(route = "splash"),
    OnBoarding(route = "onBoarding"),
    Diary(route = "diary"),
    Calendar(route = "calendar"),
    Insights(route = "insights"),
    Settings(route = "settings"),
    AddingNote(route = "addingNote"),
    Themes(route = "themes")
}